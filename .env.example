# PostgreSQL connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=studyguide
DB_USER=postgres
DB_PASSWORD=your_password_here

# AI provider keys (all optional — mocks are used if absent)
OPENAI_API=
GEMINI_API=
DEEPSEEK_API=
